var bio = {
  "name" : "Steven Paul Jobs",
  "role" : "Genius",
  "contacts" : {
    "mobile" : "4089961010",
    "email" : "steve@mac.com",
    "github" : "Steve",
    "twitter" : "@Steve",
    "location" : "Cupertino, CA 95014"
  },
  "welcomeMessage" : "Innovation distinguishes between a leader and a follower.",
  "skills" : [
    "That vision thing", "public speaking", "motivating teams", "helping to create really amazing products"
  ],
  "biopic" : "images/stevejobs.png"
};

var education = {
  "schools" : [
    {
      "name" : "Reed College",
      "location" : "3203 SE Woodstock Blvd, Portland, OR 97202, USA",
      "degree" : "no degree earned",
      "majors" : ["Caligraphy"],
      "dates" : "1973",
      "url" : "http://www.reed.edu/"
    },
    {
      "name" : "Homestead High School",
      "location" : "21370 Homestead Rd, Cupertino, CA 95014, USA",
      "degree" : "High School Diploma",
      "majors" : ["Nature Sciences"],
      "dates" : "1969-1972",
      "url" : "http://www.hhs.fuhsd.org/"
    }
  ],
  "onlineCourses" : [
    {
      "title" : "Nanodegree Front End Web Development",
      "school" : "Udacity",
      "dates" : "2011",
      "url" : "https://www.udacity.com/course/front-end-web-developer-nanodegree--nd001"
    }
  ]
};

var work = {
  "jobs" : [
    {
      "employer" : "Apple Computer Inc.",
      "title" : "CEO",
      "location" : "1 Infinite Loop, Cupertino, CA 95014",
      "dates" : "1997-2011",
      "description" : "Helped company to once again create phenomenal products such as IMac, iBook, G4, Power Book, iTools, iMovie, Mac OS X, IPhone, IPod. We create devices that are fun, powerful and easy to use."
    },
    {
      "employer" : "Pixar Animation Studio",
      "title" : "CEO",
      "location" : "1200 Park Avenue Emeryville, California 94608",
      "dates" : "1986-2006",
      "description" : "Discovered a little animation company that needed a vision. Liked the product so much that I bought the company. Pixar is now the digital animation studio. We have created and produced half of the top grossing animated films of all time, including: Toy story, A Bug's Life, Toy story 2 and Monsters, Inc. Sold the company to Disney in 2006."
    },
    {
      "employer" : "NeXT Software Inc.",
      "title" : "CEO",
      "location" : "900 Chesapeake Drive, Redwood City, CA 94063 USA.",
      "dates" : "1986-1997",
      "description" : "Founded NeXT Software Inc., which created really cool hardware that exploited the full potential of Object Oriented technologies. NeXT technologies made programmers lives a whole lot easier, thus giving them more leisure time to go catch the latest Pixar release. Sold NeXT to apple in 1997."
    },
    {
      "employer" : "Apple Computer Inc.",
      "title" : "Co-Founder",
      "location" : "1 Infinite Loop, Cupertino, CA 95014",
      "dates" : "1976-1986",
      "description" : "Invested heavily in funding start-up company (Sold my VW mini-bus). Apple ignited the personal computer revolution with insanely great products such as the Apple II. Took the company public in 1980 at $22 per share. Later in 1984, we reinvented the personal computer with the Macintosh. Left in 1986 to decide which step to take NeXT."
    }
  ]
};

var projects = {
  "projects" : [
    {
      "title" : "Apple Macintosh",
      "dates" : "1984",
      "description" : "The 1984 Apple Macintosh computer was a launching point for influential products to come. The design fortified Apple's simplistic style, while uniquely sporting a separate keyboard and mouse. With 1 million units sold, consumers jumped at the chance to use it for business, finance, education, graphic design, and more. When the original 128k model hit stores, consumers were excited about a machine with a 32-bit processor and 512k maximum RAM. Compare that to the 2013 Mac Pro, which offers up to a 3.5 GHz 6-core processor and up to 64 GB of RAM.",
      "images" : [
        "images/mac1.png",
        "images/mac2.png"
      ]
    },
    {
      "title" : "Macintosh PowerBook",
      "dates" : "1991",
      "description" : "Apple's release of the portable Macintosh PowerBook in 1991 transformed the style and functionality of on-the-go computing. The PowerBook featured a highly compact design for its time, with a built-in rolling trackball, as well as keys situated close to the screen and enough space for the palms to rest comfortably. After an influential 15-year production run, the PowerBook line ended in 2006, superseded by the powerful MacBook Pro.",
      "images" : [
        "images/powerbook1.png",
        "images/powerbook2.png"
      ]
    },
    {
      "title" : "iMac",
      "dates" : "1998",
      "description" : "A colorful step forward, the release of Apple's iMac in 1998 celebrated a union of creativity and communication. The name itself — iMac with a lowercase i — symbolized the importance of the Internet. The semi-clear shell in vibrant Bondi blue allowed consumers to peek into its inner workings and was later available in a total of 13 energetic color options. With plug-and-play simplicity, the iMac took the personal computing market by stylish storm, capturing the hearts of students in particular.",
      "images" : [
        "images/imac1.png",
        "images/imac2.png"
      ]
    },
    {
      "title" : "iPod",
      "dates" : "2001",
      "description" : "In the same year that Apple introduced iTunes, the iPod burst on the scene. The sleek MP3 player brought portable pocket-sized music to shoppers around the globe, simplifying the way people purchased and listened to tunes. With its iconic click-wheel, 10-hour battery life, and space for 1,000 songs, the device had both consumers and competitors scrambling for a listen.",
      "images" : [
        "images/ipod1.png",
        "images/ipod2.png"
      ]
    },
    {
      "title" : "iPhone",
      "dates" : "2007",
      "description" : "The iPhone is undoubtedly one of Apple's most revolutionary devices to date, accounting for more than 50 percent of the company's revenue. After the initial release in 2007, the iPhone has seen several generations that continuously lead in smartphone sale (iPhone 5s 150.2 million sold). Coveted for its intuitive operating system, sleek design, and wide array of apps, the iPhone remains cutting edge.",
      "images" : [
        "images/iphone1.png",
        "images/iphone2.png"
      ]
    },
    {
      "title" : "iPad",
      "dates" : "2010",
      "description" : "Apple made waves in 2010 when it unveiled the iPad, and competitors were left scrambling to get in on the next wave of portable technology. The iPad combined the functionality of an iPod with the versatility of a larger screen and the power of portable computing. The subsequent introduction of the iPad Air in 2013 sent a hefty reverb through the tablet world, with a design almost half the weight of its predecessor and LTE connectivity to boot.",
      "images" : [
        "images/ipad1.png",
        "images/ipad2.png"
      ]
    },
    {
      "title" : "Apple Watch",
      "dates" : "2015",
      "description" : "The Apple Watch, which works in conjunction with the iPhone 5 and later models, redefines what the modern consumer expects from a timepiece. In addition to telling time, it also supports calls, texts, and apps. One of the most notable features is the Digital Touch messaging system, which turns simple finger taps into efficient communication. With three distinct design collections all sporting a sturdy sapphire display, the Apple Watch brings together the company's pioneering technology with sleek style and imaginative function.",
      "images" : [
        "images/watch1.png",
        "images/watch2.png"
      ]
    }
  ]
};

/* ****** Methods ****** */

// create display() for bio and add to corresponding object
bio.display = function() {

  // Extract - replace data and prepend HTML bio.role
  var formattedRole = HTMLheaderRole.replace("%data%", bio.role);
  $("#header").prepend(formattedRole);

  // Extract - replace data and prepend HTML bio.name
  var formattedName = HTMLheaderName.replace("%data%", bio.name);
  $("#header").prepend(formattedName);

  // Extract - replace data and append HTML bio.contacts.mobile
  var formattedMobile = HTMLmobile.replace("%data%", bio.contacts.mobile);
  $("#topContacts").append(formattedMobile);
  $("#footerContacts").append(formattedMobile);

  // Extract - replace data and append HTML bio.contacts.email
  var formattedEmail = HTMLemail.replace("%data%", bio.contacts.email);
  $("#topContacts").append(formattedEmail);
  $("#footerContacts").append(formattedEmail);

  // Extract - replace data and append HTML bio.contacts.github
  var formattedGit = HTMLgithub.replace("%data%", bio.contacts.github);
  $("#topContacts").append(formattedGit);
  $("#footerContacts").append(formattedGit);

  // Extract - replace data and append HTML bio.contacts.twitter
  var formattedTwitter = HTMLtwitter.replace("%data%", bio.contacts.twitter);
  $("#topContacts").append(formattedTwitter);
  $("#footerContacts").append(formattedTwitter);

  // Extract - replace data and append HTML bio.contacts.location
  var formattedLocation = HTMLlocation.replace("%data%", bio.contacts.location);
  $("#topContacts").append(formattedLocation);
  $("#footerContacts").append(formattedLocation);

  // Extract - replace data and append HTML bio.biopic
  var formattedBioPic = HTMLbioPic.replace("%data%", bio.biopic);
  $("#header").append(formattedBioPic);

  // Extract - replace data and append HTML bio.welcomeMessage
  var formattedWelcomeMsg = HTMLwelcomeMsg.replace("%data%", bio.welcomeMessage);
  $("#header").append(formattedWelcomeMsg);

  // Append HTML for skills heading and skills ul
  $("#header").append(HTMLskillsStart);

  // iterating through bio.skills[] and building the skills list
  if (bio.skills.length > 0) {
    for (var i = 0; i < bio.skills.length; i++) {
      var formattedSkill = HTMLskills.replace("%data%", bio.skills[i]);
      $("#skills").append(formattedSkill);
    }
  }
};

// create display() for work and add to corresponding object
work.display = function() {
  // Let's see if there are jobs - if not - don't bother the browser with loop
  if (work.jobs.length > 0) {
    // iterate through work.jobs[]
    for (var i = 0; i < work.jobs.length; i++) {
      // first there is a containing div for every job I had
      $("#workExperience").append(HTMLworkStart);

      // create formattedEmployerTitle and write to HTML
      var formattedEmployer = HTMLworkEmployer.replace("%data%", work.jobs[i].employer);
      var formattedTitle = HTMLworkTitle.replace("%data%", work.jobs[i].title);
      var formattedEmployerTitle = formattedEmployer + formattedTitle;
      $(".work-entry:last").append(formattedEmployerTitle);

      // create formattedDates and write to HTML
      var formattedDates = HTMLworkDates.replace("%data%", work.jobs[i].dates);
      $(".work-entry:last").append(formattedDates);

      // create formattedLocation and write to HTML
      var formattedLocation = HTMLworkLocation.replace("%data%", work.jobs[i].location);
      $(".work-entry:last").append(formattedLocation);

      //create formattedDescription and write to HTML
      var formattedDescription = HTMLworkDescription.replace("%data%", work.jobs[i].description);
    $(".work-entry:last").append(formattedDescription);
    }
  }
};

// create display() for projects and add to corresponding object
projects.display = function() {
  // Are there any projects? If not, don't bother the browser any longer
  if (projects.projects.length > 0) {
    // iterate through projects.projects[]
    for (var i = 0; i < projects.projects.length; i++) {
      // first there is an empty div for every project I made
      $("#projects").append(HTMLprojectStart);

      //create formattedTitle and write to HTML
      var formattedTitle = HTMLprojectTitle.replace("%data%", projects.projects[i].title);
      $(".project-entry:last").append(formattedTitle);

      // create formattedDates and write to HTML
      var formattedDates = HTMLprojectDates.replace("%data%", projects.projects[i].dates);
      $(".project-entry:last").append(formattedDates);

      // create formattedDescription and write to HTML
      var formattedDescription = HTMLprojectDescription.replace("%data%", projects.projects[i].description);
      $(".project-entry:last").append(formattedDescription);

      // Let's see if there are any project images
      if (projects.projects[i].images.length > 0) {
      // Tricky nested loop ahead to get the project images
        for (var j = 0; j < projects.projects[i].images.length; j++) {
          //create formattedImage and write to HTML
          var formattedImage = HTMLprojectImage.replace("%data%", projects.projects[i].images[j]);
          $(".project-entry:last").append(formattedImage);
        }
      }
    }
  }
};

// create display() for education and add to corresponding object
education.display = function() {
  // Are the any schools? - if not - don't bother browser with loop
  if (education.schools.length > 0) {
    // iterate through education.schools[]
    for(var i = 0; i < education.schools.length; i++) {
      // first there is a containing div for every job I had
      $("#education").append(HTMLschoolStart);

      // create formattedNameDegree and write to HTML
      var formattedName = HTMLschoolName.replace("%data%", education.schools[i].name);
      var formattedDegree = HTMLschoolDegree.replace("%data%", education.schools[i].degree);
      var formattedNameDegree = formattedName + formattedDegree;
      $(".education-entry:last").append(formattedNameDegree);

      // create formattedDates and write to HTML
      var formattedDates = HTMLschoolDates.replace("%data%", education.schools[i].dates);
      $(".education-entry:last").append(formattedDates);

      // create formattedLocation and write to HTML
      var formattedLocation = HTMLschoolLocation.replace("%data%", education.schools[i].location);
      $(".education-entry:last").append(formattedLocation);

      if (education.schools[i].majors.length > 0) {
      // Tricky nested loop ahead to get the schools mayors
        for (var j = 0; j < education.schools[i].majors.length; j++) {
          //create formattedMajor and write to HTML
          var formattedMajor = HTMLschoolMajor.replace("%data%", education.schools[i].majors[j]);
          $(".education-entry:last").append(formattedMajor);
        }
      }
    }
  }
  // Are the any projects? - if not - don't bother browser with loop
  if (education.onlineCourses.length > 0) {
    // Start off with the onlineCourses Heading
    $("#education").append(HTMLonlineClasses);

    // iterate through education.onlineCourses
    for(var k = 0; k < education.onlineCourses.length; k++) {
      // first there is a containing div for every course I took
      $("#education").append(HTMLschoolStart);

      // create formattedTitleSchool and write to HTML
      var formattedTitle = HTMLonlineTitle.replace("%data%", education.onlineCourses[k].title);
      var formattedSchool = HTMLonlineSchool.replace("%data%", education.onlineCourses[k].school);
      var formattedTitleSchool = formattedTitle + formattedSchool;
      $(".education-entry:last").append(formattedTitleSchool);

      // create formattedDates and write to HTML
      var formattedCourseDates = HTMLonlineDates.replace("%data%", education.onlineCourses[k].dates);
      $(".education-entry:last").append(formattedCourseDates);

      // create formattedUrl and write to HTML
      var formattedUrl = HTMLonlineURL.replace("%data%", education.onlineCourses[k].url);
      $(".education-entry:last").append(formattedUrl);
    }
  }
};

function displayMap() {
  $("#mapDiv").append(googleMap);
}

// Like Bob the Builder - I create the page
bio.display();
work.display();
projects.display();
education.display();
displayMap();


